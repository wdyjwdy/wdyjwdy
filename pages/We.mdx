import { Chat, Timer , Love, Map, Todo } from '/components'
import { chatItems } from '/constants/datas'

# 我们
<div style={{ display: 'flex', gap: 10, flexWrap: 'wrap' }}>
  <Chat>
    {chatItems.map(item => (
      <Chat.Item
        key={item.id} name={item.name}
        text={item.text} right={item.right}
      />
    ))}
  </Chat>
  <Timer />
  <Love />
  <Todo />
  <Map selected={['四川', '重庆', '湖南']} />
</div>
