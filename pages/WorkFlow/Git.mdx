import { Steps } from 'nextra/components'
import { FileTree } from 'nextra/components'

## Git开发流程
<Steps>
### 第一步
将远程仓库中的代码**拉取**到本地
<FileTree>
  <FileTree.Folder name="pages" defaultOpen>
    <FileTree.File name="index.html" />
  </FileTree.Folder>
</FileTree>
```bash
git clone github.com/myWeb.git
```
### 第二步
新建一个需求**分支**，在该分支中开发新的需求
```bash
git branch feature/add_readme # 新建分支 feature/add_readme
git checkout feature/add_readme # 切换到该分支
```
### 第三步
**修改**本地仓库中的代码
<FileTree>
  <FileTree.Folder name="pages" defaultOpen>
    <FileTree.File name="index.html" />
    <FileTree.File name="README.md" active />
  </FileTree.Folder>
</FileTree>
### 第四步
将准备提交的文件放到**暂存**区
```bash
git add pages/README.md
```
### 第五步
**提交**暂存区中的所有文件，并为本次提交添加一条简介
```bash
git commit -m '添加README文件'
```
### 第六步
将本地仓库中该分支的提交**推送**到远程仓库
```bash
git push origin feature/add_readme # origin 表示当前远程仓库地址
```
### 第七步
完成需求后，在本地，将需求分支**合并**到主分支，然后**删除**需求分支
```bash
git checkout main # 先切换回主分支
git merge feature/add_readme # 然后将需求分支合并到主分支
git branch -d feature/add_readme # 最后删除需求分支
```
</Steps>
## Git文件状态
### 查看状态
```bash
git status -s
```
## Git分支
### 分支操作
#### 新建分支
```bash
git branch [branch_name]
```
#### 切换分支
```bash
git checkout [branch_name]
```
#### 新建分支，并切换到该分支
等价于 `git branch` 加 `git checkout`
```bash
git checkout -b [branch_name]
```
## Git常用命令
### 暂存所有文件
```bash
git add .
```
### 直接提交所有修改过的文件
```bash
git commit -a -m [commit_message]
```
和下面命令等价
```bash
git add --update # 将所有修改过的文件放入暂存区
git commit -m [commit_message]
```